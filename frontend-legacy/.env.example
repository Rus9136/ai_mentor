# Environment Variables Example
# Copy to .env.local for local development overrides

# Backend API URL
VITE_API_URL=http://localhost:8000

# Note:
# - .env.development is used automatically in dev mode (npm run dev)
# - .env.production is used automatically in production build (npm run build)
# - .env.local overrides both and is gitignored
