# Инструкции по тестированию Фазы 3: Layout и навигация

**Дата:** 2025-10-30
**Статус:** ✅ Реализация завершена, готово к тестированию

---

## 📋 Что было реализовано

### Новые компоненты:

1. **`frontend/src/layout/Menu.tsx`** - боковое меню с навигацией
   - Пункты: Главная, Школы, Учебники (disabled), Тесты (disabled)
   - Условный рендеринг для SUPER_ADMIN
   - Иконки Material-UI

2. **`frontend/src/layout/AppBar.tsx`** - верхняя панель
   - Имя пользователя (first_name + last_name)
   - Роль пользователя (badge с цветом)
   - Кнопка Logout

3. **`frontend/src/layout/Layout.tsx`** - главный layout
   - Интеграция AppBar и Menu

4. **`frontend/src/pages/Dashboard.tsx`** - Dashboard для SUPER_ADMIN
   - 3 метрики: Всего школ, Активные, Заблокированные
   - Таблица последних 5 школ
   - Кнопка "Все школы" → переход на `/schools`

5. **Обновлён `frontend/src/App.tsx`**
   - Добавлен кастомный Layout
   - Добавлен Dashboard
   - Добавлены placeholder ресурсы (textbooks, tests)

---

## 🚀 Подготовка к тестированию

### 1. Убедитесь, что Backend запущен

```bash
# Проверка health endpoint
curl http://localhost:8000/health

# Ожидаемый ответ:
# {"status":"healthy","version":"0.1.0","project":"AI Mentor"}
```

Если backend не запущен:
```bash
cd backend
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### 2. Убедитесь, что Frontend dev server запущен

```bash
# Проверка
curl -I http://localhost:5174/

# Если не запущен:
cd frontend
npm run dev
```

Frontend будет доступен на: **http://localhost:5174**

---

## ✅ Чеклист тестирования

### Шаг 1: Проверка входа

1. ✅ Открыть http://localhost:5174
2. ✅ Должна отобразиться форма входа (React Admin login page)
3. ✅ Ввести тестовые данные:
   - **Email:** `admin@test.com`
   - **Password:** `admin123`
4. ✅ Нажать "Войти"
5. ✅ После успешного входа должен открыться Dashboard

---

### Шаг 2: Проверка Dashboard

**Что проверить:**

1. ✅ **Заголовок страницы:** "Главная"
2. ✅ **Метрики (3 карточки):**
   - "Всего школ" - должно быть число ≥ 3 (синяя иконка школы)
   - "Активные" - должно быть число ≥ 3 (зелёная галочка)
   - "Заблокированные" - должно быть 0 или больше (красный блок)
3. ✅ **Таблица "Последние созданные школы":**
   - Колонки: ID, Название, Код, Email, Статус, Дата создания
   - Должно быть минимум 3 школы:
     - "Valid School" (код: valid-school-123)
     - "Duplicate School" (код: test-school)
     - "Test School" (код: TEST001)
   - Статус должен быть "Активна" (зелёный фон)
4. ✅ **Кнопка "Все школы"** - должна быть справа вверху таблицы

---

### Шаг 3: Проверка AppBar (верхняя панель)

**Что проверить:**

1. ✅ **Название приложения:** "AI Mentor Admin" (слева)
2. ✅ **Имя пользователя:** должно отображаться справа
   - Иконка человека
   - Имя и фамилия (например: "Admin User")
3. ✅ **Badge с ролью:** "Суперадмин" (цветной badge)
4. ✅ **Кнопка пользователя:** при клике должно открыться меню с "Logout"

---

### Шаг 4: Проверка бокового меню (Sidebar)

**Что проверить:**

1. ✅ **Пункт "Главная":**
   - Иконка: Dashboard (квадрат с сеткой)
   - При клике → остаёмся на Dashboard
2. ✅ **Пункт "Школы":**
   - Иконка: School (здание школы)
   - При клике → переход на `/schools`
   - Должна открыться таблица со списком школ (ListGuesser)
3. ✅ **Пункт "Учебники":**
   - Иконка: MenuBook (книга)
   - Должен быть **disabled** (серый цвет, некликабельный)
4. ✅ **Пункт "Тесты":**
   - Иконка: Quiz (документ с вопросами)
   - Должен быть **disabled** (серый цвет, некликабельный)

---

### Шаг 5: Проверка навигации

**Тест навигации:**

1. ✅ Клик "Главная" → открывается Dashboard
2. ✅ Клик "Школы" → открывается таблица школ (ListGuesser)
3. ✅ Клик на кнопку "Все школы" в Dashboard → переход на `/schools`
4. ✅ Клик "Главная" → возврат на Dashboard
5. ✅ URL в браузере должен меняться:
   - Dashboard: `http://localhost:5174/`
   - Schools: `http://localhost:5174/schools`

---

### Шаг 6: Проверка Logout

**Тест выхода:**

1. ✅ Кликнуть на иконку пользователя в AppBar (справа вверху)
2. ✅ Должно открыться меню
3. ✅ Кликнуть "Logout"
4. ✅ Должен произойти выход из системы
5. ✅ Должна открыться форма входа
6. ✅ В localStorage не должно быть токенов:
   ```javascript
   // В DevTools Console:
   localStorage.getItem('access_token') // должно быть null
   localStorage.getItem('refresh_token') // должно быть null
   ```

---

### Шаг 7: Проверка RBAC (условный рендеринг)

**Примечание:** Этот тест требует создания дополнительного пользователя с ролью ADMIN.

**Создание тестового ADMIN пользователя (опционально):**
```bash
cd backend
python create_test_user.py --email admin-school@test.com --password admin123 --role admin --school-id 3
```

**Тест:**
1. ✅ Выйти из SUPER_ADMIN аккаунта
2. ✅ Войти как ADMIN (если создан)
3. ✅ Проверить, что меню отличается от SUPER_ADMIN
   - Пункты "Учебники" и "Тесты" не должны отображаться для школьного ADMIN
   - (Если не реализовано - это нормально, будет в следующих итерациях)

---

## 🐛 Известные ограничения

1. **ListGuesser для Schools:**
   - Используется временный компонент ListGuesser
   - Полноценный SchoolList будет создан в Фазе 4

2. **Placeholder ресурсы (Textbooks, Tests):**
   - Отображаются как disabled в меню
   - Не имеют компонентов List/Create/Edit
   - Будут реализованы в Итерациях 5B и 5C

3. **RBAC для School ADMIN:**
   - Меню пока одинаковое для всех ролей
   - Детальная настройка permissions будет в следующих фазах

---

## 📊 Ожидаемые результаты

### После успешного прохождения всех тестов:

- ✅ Login/Logout работает
- ✅ Dashboard отображает корректные метрики
- ✅ Таблица последних школ заполнена данными
- ✅ AppBar показывает имя и роль пользователя
- ✅ Боковое меню с 4 пунктами (2 активных, 2 disabled)
- ✅ Навигация между страницами работает
- ✅ Весь UI на русском языке
- ✅ TypeScript компилируется без ошибок
- ✅ Build проходит успешно

---

## 🔧 Отладка проблем

### Проблема 1: Dashboard не загружает данные

**Симптом:** Метрики показывают 0, таблица пустая

**Решение:**
```bash
# Проверить backend API
curl http://localhost:8000/api/v1/admin/schools \
  -H "Authorization: Bearer <YOUR_TOKEN>"

# Проверить консоль браузера (DevTools) на наличие ошибок CORS или 401
```

### Проблема 2: Login не работает

**Симптом:** Форма входа не принимает credentials

**Решение:**
```bash
# Проверить, что тестовый пользователь существует
cd backend
python create_test_user.py --email admin@test.com --password admin123 --role super_admin

# Проверить backend logs
```

### Проблема 3: TypeScript ошибки в браузере

**Решение:**
```bash
cd frontend
npm run build

# Проверить вывод на наличие ошибок компиляции
```

---

## 📝 Следующие шаги (Фаза 4)

После успешного тестирования Фазы 3, следующая задача:

**Фаза 4: Schools CRUD (2 дня)**
- SchoolList - полноценная таблица с фильтрами
- SchoolCreate - форма создания школы
- SchoolEdit - форма редактирования + блокировка
- SchoolShow - детальный просмотр

---

## ✅ Критерии приёмки Фазы 3

- [x] Layout с боковым меню создан
- [x] AppBar с user info и logout создан
- [x] Dashboard с метриками работает
- [x] Таблица последних школ отображается
- [x] Навигация между страницами работает
- [x] Условный рендеринг меню (usePermissions)
- [x] Весь текст на русском языке
- [x] TypeScript компилируется без ошибок
- [x] Build проходит успешно

---

**Статус:** ✅ Фаза 3 реализована и готова к тестированию
**Дата завершения:** 2025-10-30
**Следующая фаза:** Фаза 4 - Schools CRUD
